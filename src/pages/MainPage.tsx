import React, { FC } from 'react';
import { TcpSettings } from '../container/tcp-settings';

const MainPage: FC = () => {
  return (
    <div>
      <TcpSettings />
    </div>
  );
};

export { MainPage };
